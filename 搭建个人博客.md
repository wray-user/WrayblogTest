

##  技术栈选型

- **前端框架**：React
- **后端框架**：Express + Node.js
- **数据库**：MongoDB + Mongoose
- **接口设计**：RESTful API
- **工具链**：Postman、Nodemon、MongoDB Compass



## 项目结构目录

```perl
wrayblog/
├─ client/                         # 前端：Vite + React
│  ├─ public/                       # 静态资源，原样打包，不经过 JS 处理
│  │  └─ favicon.ico
│  │
│  ├─ src/                          # 前端源码
│  │  ├─ api/                       # 请求接口封装
│  │  │  ├─ request.js              # axios / fetch 基础封装
│  │  │  ├─ postApi.js              # 文章相关接口
│  │  │  ├─ authApi.js              # 登录、鉴权接口
│  │  │  ├─ commentApi.js           # 评论接口
│  │  │  └─ uploadApi.js            # 文件上传接口
│  │  │
│  │  ├─ assets/                    # 图片、图标、字体等资源
│  │  │  ├─ images/
│  │  │  ├─ icons/
│  │  │  └─ fonts/
│  │  │
│  │  ├─ components/                # 公共组件
│  │  │  ├─ Layout/                 # 页面整体布局
│  │  │  ├─ Header/                 # 顶部导航栏
│  │  │  ├─ Footer/                 # 页脚
│  │  │  ├─ PostCard/               # 文章卡片
│  │  │  ├─ MarkdownRenderer/       # Markdown 渲染组件
│  │  │  ├─ UploadBox/              # 上传组件
│  │  │  └─ Loading/                # 加载状态组件
│  │  │
│  │  ├─ pages/                     # 页面组件
│  │  │  ├─ Home/                   # 首页
│  │  │  │  ├─ index.jsx
│  │  │  │  └─ Home.module.css
│  │  │  ├─ PostDetail/             # 文章详情页
│  │  │  │  ├─ index.jsx
│  │  │  │  └─ PostDetail.module.css
│  │  │  ├─ Category/               # 分类页
│  │  │  ├─ Tags/                   # 标签页
│  │  │  ├─ About/                  # 关于页
│  │  │  ├─ Login/                  # 后台登录页
│  │  │  ├─ Admin/                  # 后台管理首页
│  │  │  ├─ PostEditor/             # 文章编辑器页面
│  │  │  └─ NotFound/               # 404 页面
│  │  │
│  │  ├─ router/                    # 前端路由配置
│  │  │  └─ index.jsx
│  │  │
│  │  ├─ styles/                    # 全局样式
│  │  │  ├─ global.css              # 全局基础样式
│  │  │  ├─ variables.css           # CSS 变量，如颜色、间距
│  │  │  └─ reset.css               # 默认样式重置
│  │  │
│  │  ├─ utils/                     # 前端工具函数
│  │  │  ├─ formatDate.js           # 时间格式化
│  │  │  ├─ storage.js              # localStorage 封装
│  │  │  └─ slug.js                 # 标题转 slug
│  │  │
│  │  ├─ hooks/                     # 自定义 React Hooks
│  │  │  ├─ useAuth.js              # 登录状态
│  │  │  └─ useTheme.js             # 主题模式
│  │  │
│  │  ├─ App.jsx                    # React 根组件
│  │  ├─ main.jsx                   # React 入口文件
│  │  └─ index.css                  # Vite 默认入口样式，可改成引入 global.css
│  │
│  ├─ .env.development              # 本地开发环境变量
│  ├─ .env.production               # 生产环境变量
│  ├─ index.html                    # Vite 入口 HTML
│  ├─ package.json                  # 前端依赖和命令
│  ├─ package-lock.json             # 自动生成的依赖锁定文件
│  ├─ vite.config.js                # Vite 配置文件
│  └─ build/                        # 打包后生成，运行 npm run build 后出现
│
├─ server/                          # 后端：Node.js + Express + MongoDB + Mongoose
│  ├─ config/                       # 配置文件
│  │  ├─ db.js                      # MongoDB 连接配置
│  │  └─ env.js                     # 环境变量读取配置，可选
│  │
│  ├─ controllers/                  # 业务逻辑
│  │  ├─ postController.js          # 文章业务
│  │  ├─ authController.js          # 登录注册业务
│  │  ├─ commentController.js       # 评论业务
│  │  └─ uploadController.js        # 上传业务
│  │
│  ├─ middleware/                   # 中间件
│  │  ├─ authMiddleware.js          # JWT 鉴权
│  │  ├─ uploadMiddleware.js        # multer 上传配置
│  │  ├─ errorMiddleware.js         # 统一错误处理
│  │  └─ notFoundMiddleware.js      # 404 处理
│  │
│  ├─ models/                       # Mongoose 数据模型
│  │  ├─ User.js                    # 用户模型
│  │  ├─ Post.js                    # 文章模型
│  │  ├─ Comment.js                 # 评论模型
│  │  └─ Category.js                # 分类模型，可选
│  │
│  ├─ routes/                       # 路由接口
│  │  ├─ index.js                   # 路由总入口，可选
│  │  ├─ postRoutes.js              # 文章接口
│  │  ├─ authRoutes.js              # 登录接口
│  │  ├─ commentRoutes.js           # 评论接口
│  │  └─ uploadRoutes.js            # 上传接口
│  │
│  ├─ services/                     # 服务层，可选
│  │  ├─ tokenService.js            # token 生成与校验
│  │  └─ uploadService.js           # 上传相关处理
│  │
│  ├─ utils/                        # 后端工具函数
│  │  ├─ generateToken.js           # 生成 JWT
│  │  ├─ response.js                # 统一响应格式
│  │  └─ slug.js                    # 标题转 slug
│  │
│  ├─ uploads/                      # 上传文件
│  │  ├─ images/                    # 上传图片
│  │  └─ videos/                    # 上传视频
│  │
│  ├─ .env                          # 后端环境变量，本地使用，不上传 Git
│  ├─ .env.example                  # 环境变量示例，可以上传 Git
│  ├─ server.js                     # 后端入口文件
│  ├─ package.json                  # 后端依赖和命令
│  └─ package-lock.json             # 自动生成的依赖锁定文件
│
├─ README.md                        # 项目说明文档
├─ .gitignore                       # Git 忽略规则
└─ .git/                            # Git 仓库目录，自动生成

```





## 后端项目搭建（Node.js）

### 1.初始化项目

```powershell 
mkdir server
cd server
npm init -y  # 初始化项目， yes(跳过一堆初始化的问题，比如项目名称，版本号等)。默认生成一个 package.json文件
npm install express mongoose cors body-parser  # 安装了四个依赖包
```

- Express：后端框架，帮助简化处理网页请求（GET、POST）、设置路由等操作。
- Mongoose：MongoDB的翻译官。Mongoose可以让你在 JavaScript 对象的形式来操作数据库。
- Cors：处理“跨域”问题。默认情况下，浏览器会阻止（通常在3000端口）去访问你的 Node.js后端（通常在 5000端口）。安装这个包并开启，你的前后端才能正常通信。
- Dody-parser：解析请求体。当你在完成一篇 Markdown 博客并点击“提交”时，文章内容汇包裹在请求包里。这个工具负责把这些原始数据“翻译”成后端能直接读取的 JSON格式。



### 2. 建立数据库连接

```js 
//db.js 
const mongoose = require('mongoose')   // 引入 mongoose 模块
mongoose.connect('mongodb://localhost:', {    // 调用连接函数，调用并打开数据库
    useNewUrlParser: true,   // 使用新的 URL 解析器，确保能正确解析复杂的连接字符串。
    useUnifiedTopology: true   // 使用新的拓扑结构引擎，让连接更稳定，防止意外掉线。
})  
```

- **`mongodb://`**：协议名称，告诉程序我们要用 MongoDB 的方式通信。
- **`localhost:27017`**：数据库的位置。`localhost` 表示就在你这台电脑上，`27017` 是 MongoDB 默认的“门牌号”（端口）。

- **`/blog`**：**数据库的名字**。如果你的 MongoDB 里还没有叫 `blog` 的数据库，别担心，当你第一次写入数据时，它会自动为你创建一个。



### 3.创建数据库模型（Mongoose)

```js
const mongoose = require('mongoose')

const PostSchema = new mongoose.Schema({    // 定义“模具”，规定了每一篇博客文章（Post）必须包含：
    title: String, 
    content: String,
    createAt: {  // 日期七个，
        type: Date, 
        default: Date.now   // 默认帮你填写
    }
})

module.exports = mongoose.model('Post', PostSchema)   // 把“模具”正式注册到数据库里，以后在代码中操作 Post，就代表在操作数据库里的文章集合
```

以上的代码准确的说，是在定义数据库里数据的”规格说明书“，也就是 Schema，在MongoDB这种自由派数据库里，你可以存任何东西。但为了不让博客乱套，你需要使用这段代码告诉程序：我的一篇文章必须长成什么样。



### 4. 搭建基础接口

```js 
const express = require('express')      // 初始化服务器
const mongoose = require('mongoose')    
const cors = require('cors')    
const Post = require('./models/Post')

mongoose.connect('mongodb:')

const app = express() 
app.use(cors())   // 允许 React 前端跨域访问这个后端
app.use(express.json())   // 让服务器能够听懂前端传过来的JSON格式数据

// 获取文章列表  - GET 请求
app.get('./api/posts', async(req, res) => {
    const posts = await Post.find().sort({createAt: -1})  // 去数据库里找文章， 按时间倒序排列。
    res.json(posts)    // 把找到的文件打包发给前端
})

// 创建新文章
app.post('/api/posts', async(req, res) => {
    const newPost = await Post.create(req.body)
    res.json(newPost)   // 把写好的标题和内容塞进数据库。
})

app.listen(3000, () => console.log('Server running on http://localhost:3000'))  // 启动服务，在 3000 端口开启监听。只要服务器不关，它就会一直等着处理请求。
```

这段代码是你的**后端大脑**（主入口文件），它把之前所有的零件（数据库连接、数据模型、Express框架）组装在一起，让你的服务器真正**跑起来**并能**处理请求**。

如果把你的项目比作一个餐厅：

- **MongoDB** 是仓库（存食材）。
- **Post.js (Model)** 是菜单规范。
- **这段代码 (server.js)** 就是**服务员和厨房的调度系统**。

在启动服务的时候， React 前端默认通常也是运行在 `3000` 端口。如果两个都用 3000，会发生端口冲突导致启动失败。建议把最后一行改为 `5000`： `app.listen(5000, () => console.log('Server running on http://localhost:5000'))` 这样，前端用 3000，后端用 5000，它们就能互不干扰地通信了。

此处要要注意一下 Node.js async/await 语法。（此处设置超链接)





